VITE_API_URL=http://localhost:3000
# Firebase config in this AI Studio version is stored in firebase-applet-config.json
# For Vercel, add your Firebase config values as needed if you convert firebase.ts to env variables.
